# ============================================================
# Dockerfile — Container image untuk HRSS Recommendation System
# ============================================================
#
# Multi-stage build:
# 1. Install dependencies
# 2. Copy source code
# 3. Expose port untuk Streamlit (8501) atau FastAPI (8000)
#
# FROM python:3.9-slim
# WORKDIR /app
# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt
# COPY . .
# EXPOSE 8501
# CMD ["streamlit", "run", "app/app.py", "--server.port=8501"]
#
# TODO: Implement production Dockerfile.
